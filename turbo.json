{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": ["**/.env.*local"],
	"globalEnv": ["*"],
	"cacheMaxAge": "7d",
	"cacheMaxSize": "5GB",
	"tasks": {
		"build": {
			"dependsOn": ["^build", "^generate"],
			"outputs": [".nuxt/**", ".output/**", "dist/**", ".vitepress/dist/**"]
		},
		"type-check": {
			"dependsOn": ["^generate", "generate"]
		},
		"clean": {
			"cache": false
		},
		"generate": {
			"cache": false
		},
		"dev": {
			"cache": false,
			"dependsOn": ["^generate"],
			"persistent": true
		},
		"export": {
			"outputs": ["out/**"]
		},
		"test": {
			"outputs": ["coverage/**"]
		},
		"start": {
			"cache": false,
			"dependsOn": ["^generate", "^build"],
			"persistent": true
		}
	}
}
